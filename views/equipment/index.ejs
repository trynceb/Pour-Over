<%- include('../partials/header') %>

<h1>This is the Equipment Page</h1>

<div>Brewers: </div>
        <% brewers.forEach(function(b) { %>
            <ul><%= b.name %></ul>
        <% }); %>
<div>Grinders: </div>
        <% grinders.forEach(function(g) { %>
            <ul><%= g.name %></ul>
        <% }); %>


<button><a href="/equipment/new">Add Equipment</a></button>
<button><a href="/">BACK</a></button>
<%- include('../partials/footer') %>